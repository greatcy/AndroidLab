<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eli.test">

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".ShowPhotoActivity" />

        <activity android:name=".SimplestActivity">

        </activity>

        <activity android:name=".floatwindow.FloatActivity">

        </activity>

        <service android:name=".floatwindow.FloatService" />
        <service android:name=".floatwindow.NewFloatService" />

        <service android:name=".floatwindow.AnimFloatService" />

        <activity android:name=".TestAnimActivity">

        </activity>

        <activity android:name=".SettingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".widget.TestFloatAnimActivity">

        </activity>

        <activity
            android:name=".HomeActivity"
            android:launchMode="singleInstance" />
    </application>

</manifest>